import { Poppins, Nova_Flat }from "next/font/google";

export const poppins = Poppins({ weight:"400", subsets: ['latin'] });

export const poppins_600 = Poppins({ weight:"600", subsets: ['latin'] });

export const poppins_700 = Poppins({ weight:"700", subsets: ['latin'] });

export const nova_flat = Nova_Flat({ weight:'400', subsets: ['latin']});